services:
  database:
    # Self-contained image that bakes in database/init/*.sql, so this
    # compose file works on any host without a repo checkout.
    image: ghcr.io/fbrase-itk/vehicle_configurator-database:${DATABASE_TAG:-latest}
    container_name: configurator-db
    env_file:
      - ./env/database.env
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: ghcr.io/fbrase-itk/vehicle_configurator-backend:${BACKEND_TAG:-latest}
    container_name: configurator-backend
    env_file:
      - ./env/backend.env
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    image: ghcr.io/fbrase-itk/vehicle_configurator-frontend:${FRONTEND_TAG:-latest}
    container_name: configurator-frontend
    env_file:
      - ./env/frontend.env
    # Published image serves the compiled Vue app via nginx on port 80.
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db-data:
