package com.configurator.repository;

import com.configurator.model.PaintOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaintOptionRepository extends JpaRepository<PaintOption, Integer> {
}
